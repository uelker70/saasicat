{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://saasicat.dev/schemas/plan-catalog.schema.json",
    "title": "PlanCatalog",
    "description": "Language-neutral definition of a plan catalog + app identity for a SaaS app. Maintained by the consumer as a YAML/JSON file (`config/saas.yaml`) and mirrored into platform tables at backend boot.",
    "type": "object",
    "required": ["schemaVersion", "app", "currency", "vatRate", "tenantBilling"],
    "additionalProperties": false,
    "properties": {
        "schemaVersion": {
            "type": "integer",
            "const": 1
        },
        "app": {
            "type": "object",
            "additionalProperties": false,
            "description": "App identity block (branding + version). One installation serves one application, so this is the only place it is named. Consumed by AdminPublicBootController + AdminManifestConfigFactory.",
            "required": ["name"],
            "properties": {
                "name": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Brand display name (e.g. \"My App\")."
                },
                "label": {
                    "type": "string",
                    "description": "Tag/subtitle in the brand block (e.g. \"SuperAdmin\")."
                },
                "version": {
                    "type": "string",
                    "description": "App version string (build info)."
                },
                "icon": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 8,
                    "description": "Short abbreviation for the logo badge (e.g. \"ma\", \"da\")."
                },
                "logoUrl": {
                    "type": "string",
                    "format": "uri-reference",
                    "description": "Optional URL to PNG/SVG; when set, the UI renders an <img> instead of the initials badge."
                }
            }
        },
        "currency": {
            "type": "string",
            "pattern": "^[A-Z]{3}$",
            "description": "ISO 4217 currency code. EUR-only in phase 1."
        },
        "vatRate": {
            "type": "number",
            "minimum": 0,
            "maximum": 100,
            "description": "VAT rate in percent. Required (even when 0)."
        },
        "tenantBilling": {
            "type": "object",
            "additionalProperties": false,
            "description": "Commercial settings for the tenant-facing self-service routes. Required, and required member by member: every one of these has a money or a legal consequence, and a value left out is still a decision — just an invisible one. The file is read at boot, so a change lands on the next restart.",
            "required": ["cancellationNoticeDays", "selfServiceBlockedPlans"],
            "properties": {
                "cancellationNoticeDays": {
                    "type": "object",
                    "additionalProperties": false,
                    "description": "Days of notice before a term ends, one number per rhythm. A cancellation declared after the window has closed takes effect at the first period end that actually serves the notice. Two numbers rather than one because a monthly and a yearly contract cannot share a notice period: a fortnight is unusual on a year, and three months is void against a consumer on a month. No ceiling is enforced — §309 Nr. 9 BGB caps it at one month in German consumer contracts, and an installation serving businesses is not bound by that.",
                    "required": ["monthly", "yearly"],
                    "properties": {
                        "monthly": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "Notice days on a monthly rhythm. 0 means there is no door to be shut out of: a cancellation on the last day still lands at the term end."
                        },
                        "yearly": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "Notice days on a yearly rhythm. Raise it and the cut is hard — a declaration made after the window lands one full year later."
                        }
                    }
                },
                "selfServiceBlockedPlans": {
                    "type": "object",
                    "additionalProperties": false,
                    "description": "Plans a tenant may not move to or away from without talking to sales. Both lists are required and may be empty; an empty list is the explicit statement that self-service reaches every plan.",
                    "required": ["asTarget", "asSource"],
                    "properties": {
                        "asTarget": {
                            "type": "array",
                            "items": {
                                "type": "string"
                            },
                            "description": "Plan IDs that may not be selected via self-service — typically ENTERPRISE, which only a special contract activates."
                        },
                        "asSource": {
                            "type": "array",
                            "items": {
                                "type": "string"
                            },
                            "description": "Plan IDs that may not be left via self-service — typically an active special contract, whose change goes through sales."
                        }
                    }
                }
            }
        },
        "marketing": {
            "type": "object",
            "additionalProperties": false,
            "description": "App-wide marketing configuration..",
            "required": ["availableLocales"],
            "properties": {
                "availableLocales": {
                    "type": "array",
                    "items": {
                        "type": "string",
                        "pattern": "^[a-z]{2}(-[A-Z]{2})?$"
                    },
                    "minItems": 1,
                    "uniqueItems": true,
                    "description": "Allowed language pool the app may market. First = default locale. From it the SuperAdmin activates a subset in the marketing catalog (LocaleManager)."
                }
            }
        },
        "notifications": {
            "type": "object",
            "additionalProperties": false,
            "description": "Who is told when something in this file changes between two starts. The record inside the application is written either way; mail is the addition, never the substitute. Optional: an installation of one operator who signs in daily needs nothing here.",
            "properties": {
                "settingsChanged": {
                    "type": "array",
                    "items": {
                        "type": "string",
                        "format": "email"
                    },
                    "uniqueItems": true,
                    "description": "Addresses mailed when the settings applied at a start differ from the ones applied at the previous start. Mailed only where an email port is bound (`adapters.email`); without one the boot log says so once and the change is recorded in the application only. An empty list names nobody."
                }
            }
        },
        "features": {
            "type": "array",
            "items": {
                "$ref": "#/$defs/FeatureDef"
            },
            "description": "Master list of all feature flags of the project. Plans may only reference keys declared here."
        },
        "plans": {
            "type": "array",
            "items": {
                "$ref": "#/$defs/PlanDef"
            },
            "description": "Optional. When omitted, plans come exclusively from the AdminUI / DB."
        }
    },
    "$defs": {
        "FeatureDef": {
            "type": "object",
            "required": ["key"],
            "additionalProperties": false,
            "properties": {
                "key": {
                    "type": "string",
                    "pattern": "^[A-Z][A-Z0-9_]+$",
                    "description": "SCREAMING_SNAKE_CASE; unique per catalog."
                },
                "label": {
                    "type": "string",
                    "minLength": 1
                },
                "icon": {
                    "type": "string"
                },
                "tier": {
                    "type": "string",
                    "description": "Optional logical group. Convention: CORE / ADVANCED / PRO / BUSINESS / ENTERPRISE_ONLY."
                },
                "plannedOnly": {
                    "type": "boolean",
                    "default": false,
                    "description": "Marks that the feature is not in production (no plan or bundle references it)."
                }
            }
        },
        "PlanDef": {
            "type": "object",
            "required": ["id", "quotas", "features"],
            "additionalProperties": false,
            "properties": {
                "id": {
                    "type": "string",
                    "pattern": "^[A-Z][A-Z0-9_]+$",
                    "description": "Plan ID. Freely chosen by the consumer (BASIC, STANDARD, ...). Ends up as a string in Subscription.plan."
                },
                "name": {
                    "type": "string",
                    "minLength": 1
                },
                "tagline": {
                    "type": "string"
                },
                "marketed": {
                    "type": "boolean",
                    "default": true,
                    "description": "false = not selectable in self-service onboarding (e.g. ENTERPRISE)."
                },
                "popular": {
                    "type": "boolean",
                    "default": false,
                    "description": "Highlighted card in onboarding (max. 1 per catalog)."
                },
                "monthlyNet": {
                    "type": ["number", "null"],
                    "minimum": 0,
                    "description": "Net monthly price. null = on request (sales contact)."
                },
                "yearlyNet": {
                    "type": ["number", "null"],
                    "minimum": 0,
                    "description": "Net total amount per year for BillingCycle=YEARLY (not the effective monthly price). null = monthly only."
                },
                "quotas": {
                    "type": "object",
                    "patternProperties": {
                        "^[a-z][A-Za-z0-9]*$": {
                            "type": "integer",
                            "description": "-1 = unlimited; 0 = not allowed; >0 = hard upper limit."
                        }
                    },
                    "additionalProperties": false,
                    "minProperties": 1
                },
                "features": {
                    "type": "array",
                    "items": {
                        "type": "string",
                        "pattern": "^[A-Z][A-Z0-9_]+$"
                    },
                    "uniqueItems": true,
                    "description": "List of included feature keys; all must exist in features[].key."
                }
            }
        }
    }
}
