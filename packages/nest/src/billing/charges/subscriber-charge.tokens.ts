/** Adapter token: where the subscriber's charges are written (`tenantBilling.chargeJournal`). */
export const SUBSCRIBER_LEDGER_REPOSITORY_TOKEN = Symbol.for(
    'saasicat/nest/SubscriberLedgerRepository',
);
