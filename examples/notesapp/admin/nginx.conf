server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    # Same-origin API: the SPA calls /api/v1/... and nginx forwards it, so the
    # backend needs no CORS configuration.
    location /api/ {
        # Resolved per request via Docker's embedded DNS. A literal upstream is
        # resolved once at startup, which makes nginx refuse to boot whenever
        # the backend container is not up yet.
        resolver 127.0.0.11 valid=10s ipv6=off;
        set $upstream http://backend:3000;
        proxy_pass $upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Vue router runs in history mode — unknown paths are routes, not files.
    location / {
        try_files $uri $uri/ /index.html;
    }

    # The entry document must never be cached. It is the only file whose name
    # does not change between builds, so a cached copy keeps pointing at the
    # previous build's hashed assets — which are served as `immutable` below
    # and therefore never re-fetched. Without a Cache-Control header browsers
    # fall back to heuristic caching and decide for themselves, which is how a
    # redeployed container keeps serving the old UI.
    location = /index.html {
        add_header Cache-Control "no-store, must-revalidate";
    }

    location ~* \.(?:js|css|woff2?|svg|png|jpg|ico)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
