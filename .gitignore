# Dependencies
node_modules
.pnpm-store

# Build output
dist
*.tsbuildinfo

# Caches
.cache
.turbo
.vite

# Test / coverage
coverage
# Written by scripts/coverage-ratchet.mjs on every run; uploaded as a CI
# artefact, never committed. `coverage` above does not match it.
coverage-report.json
test-results
playwright-report
.integration-tmp

# Environment & secrets
.env
*.env.local
.env.*.local

# Foreign lockfiles / debug logs (we use pnpm-lock.yaml — keep it tracked)
yarn.lock
yarn-error.log
package-lock.json
npm-debug.log

# Editor / OS
.idea
*.swp
*.swo
.DS_Store

# jscpd duplication report (see .jscpd.json)
.jscpd-report/

# Internal planning notes — not part of the published project
handoff/

# Local agent instructions. Imports handoff/, so it would be half-broken in a
# clone anyway; the contributor-facing rules live in CONTRIBUTING.md.
CLAUDE.md
